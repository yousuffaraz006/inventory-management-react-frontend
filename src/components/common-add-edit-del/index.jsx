import CommonDialog from "../common-dialog";

function CommonAddEditDel({ title, formControls, handleSubmit, buttonText }) {
  return ( 
    <CommonDialog title={t}/>
   );
}

export default CommonAddEditDel;